@inherits BreakpointBlockComponentBase
<div @attributes=GetAllAttributes()>
    @ChildContent
</div>
@code {
    protected override IDictionary<string, object> DefaultAttributes => new Dictionary<string, object>()
    {
        { "class", GetBreakpoints("col") + GetOffsets() }
    };
}